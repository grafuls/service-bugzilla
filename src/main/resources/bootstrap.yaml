info:
  build:
    name: Bugzilla Service
    description: ReportPortal Bugzilla Integration
    version: '@version@'
    branch: '@branch@'
    repo: '@repo@'

spring:
  application:
    name: bugzilla
  profiles:
    active: ${rp.profiles:default}
security:
  sessions: stateless
  basic:
    enabled: false
